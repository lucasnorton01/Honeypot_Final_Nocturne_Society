# Scripts históricos

Operaciones puntuales del 10–16/09/2026 sobre copias de la base de n8n; contienen rutas locales; no forman parte del pipeline; `update_db*.js` modificaron `nodes` y `connections` de `workflow_entity` en una copia de la base.
